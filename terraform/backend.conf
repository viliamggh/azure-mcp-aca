# Backend configuration for Terraform state
# This file should be used with `terraform init -backend-config=backend.conf`

storage_account_name = "azuremcpacast"
container_name       = "terraform-state"
key                  = "azure-mcp-aca.tfstate"
resource_group_name  = "azure-mcp-aca-rg"
use_oidc            = true
